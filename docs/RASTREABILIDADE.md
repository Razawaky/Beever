# Rastreabilidade — requisito → arquivos → testes

Tabela exigida pela seção 7 do `docs/01-REQUISITOS-E-REGRAS.md` e material
direto da defesa do TCC.

**Escopo de hoje:** o arquivo nasceu na T-06.1 e cobre, por ora, só os
requisitos tocados a partir dela. O preenchimento retroativo das etapas E01 a
E05 é a **T-15.1**, na E15 — até lá, ausência aqui não significa requisito não
atendido, significa linha ainda não escrita.

**Situação** diz o quanto do requisito está de pé: *atendido* quando o caminho
inteiro existe e tem teste; *parcial* quando a peça desta etapa existe mas
depende de tarefa seguinte para valer no jogo.

| Requisito | Arquivos | Testes | Situação |
|---|---|---|---|
| **RN-002** — XP nunca é gasto nem perdido, só acumula | `src/repositories/userLevelsRepository.js` (`lancarXp` só aceita crédito), `src/services/levelsService.js` | `test/integration/recompensaDaCelula.test.js` — "o cache de user_levels continua batendo com o livro"; `scripts/reconcile.js` | atendido |
| **RN-003** — nível vem da tabela `levels`, nunca de fórmula no código | `scripts/seeds/01_levels.sql`, `src/services/levelsService.js` (`nivelParaXp`, `xpDoProximoNivel`) | `test/unit/levelsService.test.js` — leitura da curva, incluindo salto de vários níveis | atendido |
| **RN-001** — XP, pólen e mel são recompensas independentes, sem conversão entre si | `src/services/levelsService.js`, `src/services/pointsService.js`, `src/services/coinsService.js` — um service por recompensa, e o de XP calcula o bônus de nível mas não credita mel | `test/integration/recompensaDaCelula.test.js` — "subir de nível devolve o bônus de mel da curva, sem creditar mel aqui" | atendido |
| **RN-004** — mel nunca fica negativo; débito valida saldo e é atômico | `src/repositories/walletsRepository.js` (`debitarMel`, com `WHERE coins >= ?` na mesma instrução), `src/services/coinsService.js` (`debitar`) | `test/integration/recompensaDaCelula.test.js` — "gastar mais mel do que se tem é recusado, e não deixa rastro" | atendido |
| **RN-005** — valor monetário é inteiro, nunca `FLOAT` | `migrations/014_reward_modifiers.sql` (fator em `DECIMAL`, valores em mel seguem inteiros) | `test/integration/schema.test.js` — "não tem nenhuma coluna em ponto flutuante" | atendido |
| **RN-006** — todo valor de recompensa vem de configuração em banco | `migrations/003_rewards_ledgers.sql` (`reward_configs`), `migrations/014_reward_modifiers.sql`, `scripts/seeds/04_reward_configs.sql`, `scripts/seeds/07_reward_modifiers.sql`, `src/repositories/rewardConfigsRepository.js` | `test/integration/repositories/rewardConfigs.test.js` — os 54 combos de jogo, faixa e estrelas conferidos contra a escala do seed | atendido para a célula — XP, pólen e mel saem de `reward_configs` (`calcularXpDaCelula`, `calcularPolenDaCelula`, `calcularMelDaCelula`); tarefa e meta leem os catálogos delas |
| **RN-007** — o cálculo é exclusivamente do servidor; o cliente manda respostas, nunca pontuação | `src/services/validadoresDeJogo.js` (gabarito do banco; conteúdo vai à tela sem `correta`), `src/services/gameSessionService.js` | `test/integration/sessaoDeJogo.test.js` — "a pontuação mandada pelo cliente é ignorada" e "abrir devolve token e as perguntas sem o gabarito" | atendido para o quiz, com teste unitário do validador em `test/unit/validadoresDeJogo.test.js` e `test/integration/aceiteDoMotor.test.js` — "cliente mentindo na pontuação é ignorado"; os outros cinco jogos entram na E07 |
| **RN-008** — repetir célula concluída paga 25% de XP e zero mel | `scripts/seeds/07_reward_modifiers.sql` (`repeticao-de-celula`), `src/repositories/rewardConfigsRepository.js` (`buscarModificador`), `src/services/levelsService.js` (`calcularXpDaCelula`), `src/services/pointsService.js` (`calcularPolenDaCelula`), `src/services/coinsService.js` (`calcularMelDaCelula`), `src/repositories/progressRepository.js` (estrela e pontuação só sobem) | `test/integration/repositories/rewardConfigs.test.js` — "traz o corte da repetição da RN-008 como número"; `test/integration/recompensaDaCelula.test.js` — "repetir a mesma célula paga 25% do XP" e "repetir não paga pólen nenhum" e "repetir não paga mel nenhum" | atendido — 25% de XP, zero pólen e zero mel, exercido pela partida em `test/integration/sessaoDeJogo.test.js` |
| **RN-009** — cada conclusão carrega token único e nunca credita duas vezes | `src/repositories/gameSessionsRepository.js` (`bloquearAbertaPorToken` com `FOR UPDATE`), `src/services/idempotencyService.js`, `src/repositories/idempotencyKeysRepository.js`, `src/services/gameSessionService.js` | `test/integration/sessaoDeJogo.test.js` — "reenviar o mesmo token devolve o resultado, sem creditar de novo"; `test/integration/idempotencia.test.js` — "a mesma chave compra uma vez só"; `test/integration/aceiteDoMotor.test.js` — cinco conclusões em paralelo | atendido — `test/integration/aceiteDoMotor.test.js` envia cinco conclusões em paralelo e credita uma vez |
| **RF-CON-04** — registrar progresso por célula: tentativas, erros, estrelas e tempo | `src/repositories/progressRepository.js`, `src/repositories/gameSessionsRepository.js` (`duration_seconds` calculado pelo banco), `src/services/gameSessionService.js` | `test/integration/sessaoDeJogo.test.js` — "fechar com tudo certo paga as três recompensas e grava a partida" | atendido |
| **RNF-16** — idempotência garantida em crédito de recompensa e compra | `src/services/idempotencyService.js` (reserva por `INSERT IGNORE`, dentro da transação da operação), `src/services/purchasesService.js`, `src/controllers/paginaController.js` e `src/views/pages/loja.ejs` (uma chave por renderização) | `test/integration/idempotencia.test.js` — mesma chave compra uma vez, chave nova compra de novo, chave repetida com outro pedido é recusada; `test/integration/aceiteDoMotor.test.js` — cinco compras simultâneas com a mesma chave; `test/integration/aceiteDaEconomia.test.js` | atendido |
| **RN-010** — toda alteração de XP, pólen, mel, compra e ação administrativa gera linha em `audit_logs` com ator, ação, antes/depois e data | `src/services/auditService.js` (`retratoDoSaldo`, `registrarRecompensa`), `src/services/gameSessionService.js`, `src/services/profilesService.js`, `src/services/tasksService.js`, `src/services/goalsService.js`, `src/services/purchasesService.js` | `test/integration/auditoriaDeCreditos.test.js` — "fechar a partida grava uma linha com o saldo antes e depois" e "a repetição também é registrada" | atendido |
| **RNF-17** — auditoria imutável (append-only) com antes/depois | `migrations/008_audit_immutability.sql` (gatilhos que recusam UPDATE e DELETE), `src/repositories/auditLogsRepository.js` | `test/integration/auditoriaDeCreditos.test.js` — "a trilha é imutável: não dá para alterar nem apagar linha" | atendido |
| **RNF-15** — operações com saldo em transação, com rollback em qualquer falha | `src/config/database.js` (`emTransacao`), `src/services/gameSessionService.js` (as três recompensas numa transação só), `src/services/purchasesService.js` | `test/integration/recompensaDaCelula.test.js` — "gastar mais mel do que se tem é recusado, e não deixa rastro"; `test/integration/aceiteDoMotor.test.js` | atendido |
| **RF-JOG-08** — contrato único de resultado de jogo, usado por todos os jogos | `docs/CONTRATO-DE-JOGO.md`, `src/services/validadoresDeJogo.js` (`conferirForma`, `paraJogar`, `validar`, um validador por slug), `src/services/gameSessionService.js`, `src/public/js/partida.js` e `src/public/js/resultado.js` (a mesma casca e o mesmo fim para todos) | `test/unit/validadoresDeJogo.test.js` — 35 casos, sem banco, cobrindo os quatro validadores | atendido — os quatro jogos obrigatórios entraram por este contrato sem alterá-lo; os dois P1 da T-07.7 entram igual |
| **RF-JOG-01** — Quiz do Favo, múltipla escolha com feedback imediato | `src/views/pages/celula.ejs`, `src/views/partials/jogos/quiz.ejs`, `src/public/js/quiz.js`, `src/public/js/partida.js`, `src/controllers/gameSessionsController.js`, `src/routes/partidas.js`, `src/services/validadoresDeJogo.js` | `test/integration/quizDoFavo.test.js` — 8 casos pelo HTTP; `test/unit/validadoresDeJogo.test.js` | atendido — falta só ver o gesto em navegador real (DT-22) |
| **RF-JOG-02** — Arraste e Classifique: arrastar cartas para caixas | `src/views/pages/celula.ejs`, `src/views/partials/jogos/arraste.ejs`, `src/public/js/arraste.js`, `src/public/js/partida.js`, `src/services/validadoresDeJogo.js` (validador `arraste-e-classifique`), `scripts/seeds/05_demo_content.sql` | `test/integration/arrasteEClassifique.test.js` — 7 casos pelo HTTP; `test/unit/validadoresDeJogo.test.js` — 7 casos do validador, sem banco | atendido — falta só ver o gesto em navegador real (DT-22) |
| **RNF-23** — jogo de arrastar tem alternativa por clique e teclado, com foco visível | `src/public/js/arraste.js` (`selecionar`/`colocar` servem aos três caminhos, foco devolvido à carta, jogada anunciada em `aria-live`), `src/views/partials/jogos/arraste.ejs` | `test/integration/arrasteEClassifique.test.js` — a partida fecha com a lista de caixas, que é o que os três caminhos produzem | parcial — o caminho por clique e teclado existe e é o mesmo que o arrastar usa; o gesto em si não foi visto em navegador (DT-22) |
| **RF-JOG-03** — Monte o Orçamento: distribuir uma mesada entre categorias respeitando regras | `src/views/pages/celula.ejs`, `src/views/partials/jogos/orcamento.ejs`, `src/public/js/orcamento.js`, `src/public/js/partida.js`, `src/services/validadoresDeJogo.js` (validador `monte-o-orcamento`), `scripts/seeds/05_demo_content.sql` (5 células, faixas A, B e C) | `test/integration/monteOOrcamento.test.js` — 6 casos pelo HTTP; `test/unit/validadoresDeJogo.test.js` — 7 casos do validador, sem banco | atendido — falta só ver a tela em navegador real (DT-22) |
| **RF-JOG-04** — Cofre do Tempo: decidir quanto guardar e ver o rendimento crescer em gráfico | `src/views/pages/celula.ejs`, `src/views/partials/jogos/cofre.ejs`, `src/public/js/cofre.js`, `src/public/js/partida.js`, `src/services/validadoresDeJogo.js` (validador `cofre-do-tempo` e `saldoDoCofre`), `scripts/seeds/05_demo_content.sql` (4 células, faixas A, B e C) | `test/integration/cofreDoTempo.test.js` — 6 casos pelo HTTP, incluindo "guardar cedo rende mais do que guardar tarde"; `test/unit/validadoresDeJogo.test.js` — 8 casos do validador, sem banco | atendido — falta só ver o gráfico em navegador real (DT-22) |
| **RF-JOG-05** — Mercado Esperto: escolher o melhor custo-benefício comparando preço e quantidade | `src/views/partials/jogos/mercado.ejs`, `src/public/js/mercado.js`, `src/services/validadoresDeJogo.js` (validador `mercado-esperto`, `precoPorUnidade`, `indiceDaMelhorCompra`), `scripts/seeds/05_demo_content.sql` (3 células) | `test/integration/mercadoEsperto.test.js` — 5 casos pelo HTTP, incluindo "a etiqueta mais barata não é a melhor compra"; `test/unit/validadoresDeJogo.test.js` — 6 casos do validador, sem banco | atendido — falta só ver a tela em navegador real (DT-22) |
| **RF-JOG-06** — Ordene a Prioridade: ranquear gastos por urgência e importância | `src/views/partials/jogos/ordene.ejs`, `src/public/js/ordene.js`, `src/services/validadoresDeJogo.js` (validador `ordene-a-prioridade`, contagem por par invertido), `scripts/seeds/05_demo_content.sql` (3 células) | `test/integration/ordeneAPrioridade.test.js` — 5 casos pelo HTTP, incluindo "trocar dois vizinhos custa um erro"; `test/unit/validadoresDeJogo.test.js` — 6 casos do validador, sem banco | atendido — falta só ver a tela em navegador real (DT-22) |
| **RF-MET-05** / **RN-017** — meta vencida entra em `expirada` sem punição e pode ser renovada com prazo estendido e recompensa reduzida em 50% | `src/services/goalsService.js` (`expirarVencidas`, `renovar`, `listarRenovaveis`), `src/services/goalPlannerService.js` (`planoAtual`), `src/repositories/goalsRepository.js` (`listarExpiradasRenovaveis`, `marcarRenovada`), `scripts/seeds/07_reward_modifiers.sql` (fator `meta-renovada`), `src/views/pages/metas.ejs` | `test/integration/renovacaoDeMeta.test.js` — 6 casos pelo HTTP: a oferta na tela, progresso preservado, metade da recompensa, renovação única, meta ativa não renovável e meta fora de `ativa` que não paga | atendido |
| **RF-JOG-07** — estado de jogo salvo para retomar sessão interrompida | `migrations/015_estado_de_partida.sql` (coluna `saved_state`), `src/services/validadoresDeJogo.js` (`estadoParaSalvar`), `src/services/gameSessionService.js` (`abrir` devolve a partida em andamento, `salvarEstado`), `src/repositories/gameSessionsRepository.js` (`buscarAbertaDaCelula`, `salvarEstado`), `src/routes/partidas.js` (`PUT /partidas/:token/estado`), `src/public/js/partida.js` (`salvarProgresso`) e os seis jogos | `test/integration/retomadaDePartida.test.js` — 6 casos pelo HTTP: partida nova sem estado, retomada sem abrir partida nova, rascunho que não vira nota, partida encerrada, partida de outro jogador e progresso sem fim | atendido |
| **RF-CON-05** — tela de resultado com estrelas, XP, mel, pólen e animação do mascote | `src/views/partials/jogo-resultado.ejs`, `src/public/js/resultado.js` (mapa `MASCOTES`, o único ponto de troca da arte), `src/public/js/partida.js`, `src/styles/tema.css` (`.estrela-ganha`, desligada por `prefers-reduced-motion`) | `test/integration/telaDeResultado.test.js` — 4 casos pelo HTTP: a mesma marcação em qualquer jogo, o caminho para a próxima célula, o reenvio do token e o beco evitado | atendido — falta só ver a animação em navegador real (DT-22) |
| **RNF-26** — `prefers-reduced-motion` desliga animação não essencial | `src/styles/tema.css` (`.animate-float`, `.estrela-ganha`, `.floating-indicator`, `.toast`) | conferido por leitura do CSS compilado; não há teste automatizado de mídia | atendido para o que existe hoje; cada animação nova precisa entrar no mesmo bloco |
| **RF-CON-03** — abrir a célula e jogar a atividade | `src/controllers/paginaController.js` (`celula`, mapa `TELAS_DE_JOGO`), `src/services/contentService.js` (`abrirCelula`, `temJogo`) | `test/integration/quizDoFavo.test.js` — "a célula travada não abre a página do jogo" e "a página do jogo não entrega o gabarito nem cria partida"; `test/integration/arrasteEClassifique.test.js` — "a página traz o id da célula e o token que o JavaScript lê, e eles funcionam" | atendido para o quiz e para o arrastar |
| **RF-SEQ-01** / **RN-019** a **RN-021** — avaliar a sequência: dia marcado com célula concluída avança, dia marcado em branco quebra, dia não marcado é neutro, e a verificação roda na primeira requisição depois da virada | `src/services/streakService.js` (`avaliar`, `registrarDiaCumprido`, `desfechoDoDia`), `src/repositories/streaksRepository.js`, `src/repositories/gameSessionsRepository.js` (`listarConclusoesNoIntervalo`), `src/services/gameSessionService.js` (avança ao concluir a célula), `src/controllers/paginaController.js` (avaliação no painel e nas metas), `migrations/004_goals_tasks_streaks.sql`, `scripts/seeds/02_age_bands_domains.sql` (os quatro desfechos) | `test/integration/sequencia.test.js` — 7 casos: dia marcado em branco quebra, dia de folga não quebra, dia cumprido avança, avaliar duas vezes não conta duas vezes, várias células no mesmo dia contam um dia só, o fuso decide o dia contado e o painel avalia sozinho; `test/integration/tresSemanasDeSequencia.test.js` — o aceite da etapa: vinte e um dias encadeados com o instante injetado, conferindo o desfecho e a sequência dia a dia | atendido |
| **RN-024** — o dia vira no fuso do perfil, nunca no do servidor | `src/utils/diaDoJogador.js` (`dataDoDia`, `inicioDoDia`, `inicioDaSemana`, com dupla passagem para o horário de verão), `src/services/profilesService.js` (`fusoDoUsuario`), `src/services/streakService.js`, `src/services/tasksService.js` (`garantirTarefasDoDia`), `migrations/001_core_users.sql` (`profiles.timezone`), `docker-compose.yml` (`--default-time-zone=+00:00`, para o `NOW()` do banco falar o mesmo UTC que a aplicação grava) | `test/unit/diaDoJogador.test.js` — 7 casos sem banco: o mesmo instante em dois fusos, meia-noite do fuso, semana, soma de dias, dia do ano, fuso inválido e horário de verão; `test/integration/sequencia.test.js` — "o dia contado é o do fuso do perfil"; `test/integration/tresSemanasDeSequencia.test.js` — o aceite da etapa: vinte e um dias encadeados com o instante injetado, num jogador de Nova York, atravessando a entrada do horário de verão em 08/03/2026 | atendido — paga a dívida DT-23 |
| **RF-SEQ-03** / **RN-022** — o Escudo de Sequência é consumido automaticamente para proteger um dia marcado perdido, com no máximo dois guardados | `src/services/streakService.js` (`escudosDisponiveis`, `sincronizarEscudos`, `consumirEscudo`, com a varredura inteira travando o jogador por `usersRepository.travarPorId`), `src/repositories/inventoryRepository.js` (`contarAtivosDoItem`, `bloquearUnidadeAtivaDoItem`, `marcarComoConsumido`), `src/repositories/streaksRepository.js` (`definirEscudos`), `src/services/purchasesService.js` (teto de dois antes do débito), `scripts/seeds/03_items_catalog.sql` (o item), `scripts/seeds/02_age_bands_domains.sql` (estados `protegido` e `consumido`), `migrations/004_goals_tasks_streaks.sql` (`CHECK (shields_available <= 2)`) | `test/integration/escudoDeSequencia.test.js` — 7 casos: o dia salvo sem o jogador pedir, duas visitas simultâneas que gastam um escudo só, a unidade que sai como consumida e não vendida, um escudo salvando um dia só, o escudo poupado quando não há sequência, a compra que atualiza a contagem e o terceiro escudo recusado com a carteira intacta; `test/integration/tresSemanasDeSequencia.test.js` — o aceite da etapa: vinte e um dias encadeados com o instante injetado, onde o único escudo é gasto no primeiro dia perdido e o segundo dia em branco quebra | atendido — a contagem de escudos guardados aparece na tela de metas desde a T-08.6 |
| **RF-SEQ-04** / **RN-023** — os marcos de 7, 14, 30, 60 e 100 dias rendem mel bônus e conquista | `scripts/seeds/08_achievements.sql` (as cinco conquistas e o valor de cada uma), `src/repositories/achievementsRepository.js`, `src/services/achievementsService.js` (desbloqueio e crédito na mesma transação, com auditoria `conquista.desbloqueada`), `src/services/streakService.js` (`conferirMarco`), `migrations/007_gamification.sql` (`user_achievements` com `UNIQUE (user_id, achievement_id)`), `scripts/seeds/02_age_bands_domains.sql` (motivo `marco-de-sequencia`) | `test/integration/marcoDeSequencia.test.js` — 6 casos: o sétimo dia paga e grava, o valor vem do banco (trocado para 777 no teste), o mesmo marco não paga duas vezes, dia comum não paga, o lançamento aparece em `coin_ledger` com o motivo certo, e o desbloqueio direto pelo service também é único; `test/integration/tresSemanasDeSequencia.test.js` — o aceite da etapa: vinte e um dias encadeados com o instante injetado, onde o marco de sete é atingido e pago uma vez ao longo da corrida | atendido — os marcos desbloqueados aparecem na tela de metas desde a T-08.6 |
| **RF-TAR-01** / **RN-047** — tarefas diárias nos dias marcados, semanais na virada, nunca mais de 3 ativas | `src/services/tasksService.js` (`garantirTarefasDoDia`, com expiração preguiçosa e teto de 3), `src/repositories/tasksRepository.js` (`expirarVencidasDoUsuario`, `contarAtivas`), `src/utils/diaDoJogador.js` (o dia e a semana no fuso do perfil), `src/controllers/paginaController.js` | `test/integration/tarefasDoDia.test.js` — dia fora da agenda não gera, três chamadas no mesmo dia não passam de 3 ativas, a vencida expira e abre vaga, e a tarefa sem fonte mensurável não é proposta | atendido |
| **RF-TAR-02** / **RN-046** — listar, atualizar progresso e concluir com recompensa | `src/services/taskProgressSources.js` (`cell_completed`, `active_days`, `hive_completed`, medidos na janela da tarefa), `src/services/tasksService.js` (`sincronizarProgresso`, `concluir`), `src/repositories/tasksRepository.js` (`definirProgresso`, `concluir` com o alvo no `WHERE`), `src/repositories/progressRepository.js` (`contarFavosConcluidosNoIntervalo`), `src/views/pages/metas.ejs` | `test/integration/tarefasDoDia.test.js` — concluir células move a tarefa sozinha, o progresso não passa do alvo nem volta atrás, e a rota do passo manual devolve 404; `test/integration/fluxoAutenticado.test.js` — cumprir pelo evento paga uma vez só; `test/integration/repositories/tasks.test.js` | atendido — paga a dívida DT-21 para as três fontes que existem; `vault_deposit` fica para a E09 |
| **RF-SEQ-02** — calendário semanal com dias marcados, cumpridos e perdidos | `src/services/streakService.js` (`resumoDaSemana`), `src/services/schedulesService.js` (`nomeDoDia`), `src/repositories/streaksRepository.js` (`listarEventos`), `src/views/partials/ui/calendario-semana.ejs`, `src/views/pages/metas.ejs`, `src/views/pages/painel.ejs`, `src/controllers/paginaController.js` | `test/integration/telaDeSequencia.test.js` — 6 casos: os sete dias de domingo a sábado com hoje marcado, os quatro desfechos no dia certo, hoje e futuro sem desfecho, dia fora da agenda como folga, e o calendário renderizado na tela de metas e no painel; `test/unit/schedulesService.test.js` — 3 casos do `nomeDoDia`: os sete dias, o dia em texto como chega do formulário e o dia fora da faixa | atendido — falta só ver o desenho em navegador real (DT-22) |
| **RF-MET-01** / **RF-MET-03** / **RN-015** — gerar meta conforme a disponibilidade, medindo o progresso de cada tipo sem nunca sortear meta impossível | `src/services/goalProgressSources.js` (`coin_balance`, `user_level`, `cell_completed`, `hive_completed`, `streak_days`), `src/repositories/progressRepository.js` (`contarCelulasConcluidas`, `contarFavosConcluidos`), `src/repositories/streaksRepository.js` (`buscarPorUsuario`, de onde sai a sequência de hoje), `src/services/goalPlannerService.js` (`TITULOS`, `calcularAlvo`, `garantirMetasAtivas`), `scripts/seeds/02_age_bands_domains.sql` (réguas de alvo dos cinco tipos mensuráveis) | `test/integration/planejadorDeMetas.test.js` — 7 casos: as três faixas da RN-014, o assunto que não se repete havendo tipo livre, o tipo sem fonte que não é sorteado, a leitura real de célula, favo e sequência, a idempotência do painel e a reposição da meta concluída | parcial — cinco dos sete tipos medem; `patrimony_total` e `vault_balance` entram na E09 |
| **RN-042** a **RN-044** — o cofre guarda mel, rende por ciclo, aceita saque livre e pode ter meta com prazo | `migrations/005_economy_items_inventory.sql` (`vaults`, `vault_transactions`), `src/repositories/vaultsRepository.js` (`creditar`, `debitar` com `balance >= ?` no `WHERE`, `registrarTransacao` com `balance_after`, `totalSacadoDesde` para a RN-043, `definirMeta`), `scripts/seeds/02_age_bands_domains.sql` (os quatro tipos de movimento) | `test/integration/repositories/vaults.test.js` — 7 casos: cofre zerado com a taxa da RN-042, criação idempotente, depósito com saldo no extrato, saque acima do saldo recusado, saque do saldo inteiro, soma dos saques do ciclo e meta definida e apagada | parcial — o repository está de pé; depósito, saque, rendimento e projeção são o `VaultService` da T-09.4 |
| **Aceite da E09** — entrar após 6 semanas sem acessar aplica todos os ciclos uma única vez, com extrato claro e nada de saldo negativo | `src/services/economicCycleService.js`, `src/services/purchasesService.js`, `src/services/vaultService.js`, `src/services/patrimonyService.js`, `src/controllers/paginaController.js` (`painel`) | `test/integration/aceiteDaEconomia.test.js` — 9 casos sobre o mesmo jogador: compra recusada por falta de mel sem rastro, dois cliques comprando uma vez só pelo HTTP, mel guardado sem mudar o patrimônio, seis ciclos aplicados numa visita, segunda visita sem efeito, um rendimento de cofre por ciclo, item vendido por 50% depois de duas semanas devendo, nenhuma linha negativa no `coin_ledger` e o patrimônio igual à soma das partes. O tempo da visita mais pesada é asserção, com teto de 2 s (RNF-01) | atendido |
| **RN-036** — o ciclo econômico é semanal, processado de forma preguiçosa e aplicado uma única vez | `migrations/005_economy_items_inventory.sql` (`economic_cycles`, com `UNIQUE (user_id, cycle_number)`), `src/repositories/economicCyclesRepository.js` (`registrar` por `INSERT IGNORE`, `gravarResumo`, `ultimoNumeroProcessado`, `listarUltimos`), `src/services/economicCycleService.js` (`numeroDoCiclo` pelo calendário do jogador, `separarPendentes` com teto de doze por visita, `processarPendentes` com uma transação por ciclo e a marca antes dos efeitos), `src/controllers/paginaController.js` (a Colmeia processa antes de ler saldo) | `test/unit/economicCycleService.test.js` — 8 casos sem banco: a conta nova no ciclo zero, a virada no domingo, seis semanas fora, o mesmo número em duas aberturas do mesmo dia, o fuso decidindo quem já virou a semana e o teto de doze; `test/integration/cicloEconomico.test.js` — 9 casos contra banco real: seis semanas fora que rodam uma vez só, duas abas simultâneas que aplicam um ciclo só, e os ciclos acima do teto marcados sem efeito | atendido — o extrato de cada ciclo vira frase na Colmeia (RF-HOM-09) |
| **RF-HOM-09** — aviso na Colmeia dos eventos do ciclo (rendimento do cofre, custo cobrado, renda recebida, item vendido) | `src/services/economicCycleService.js` (`avisoDosCiclos`, conta pura que soma vários ciclos num aviso só; `avisoDoDia`, que desde a T-10.5 monta o destaque a partir do `processed_at` do dia do jogador e paga a DT-63; e `listarEventosRecentes`, que lê o `summary` gravado por ciclo), `src/repositories/economicCyclesRepository.js` (`listarProcessadosDesde`), `src/views/partials/ui/aviso-do-ciclo.ejs`, `src/views/pages/painel.ejs`, `src/services/homeService.js` | `test/unit/avisosDoCiclo.test.js` — 9 casos sem banco: ciclo silencioso e ciclo pulado que não viram aviso, renda, custo e rendimento somados, seis ciclos num aviso só, o item devendo avisado uma vez e a venda forçada com motivo e valor; `test/integration/avisoDoCiclo.test.js` — 4 casos pelo HTTP: quem está em dia não vê aviso, quem volta depois de três semanas lê as frases, recarregar mantém a notícia do dia e dois dias depois sobra só o histórico | atendido — falta o passe de olho humano no desenho (DT-22) |
| **RN-034** / **RN-035** — todo item tem comportamento econômico, e pode ter mais de um | `scripts/seeds/03_items_catalog.sql` (`item_behaviors_map` derivado de `valuation_rate`, `upkeep_cost` e `income_per_cycle`), `src/repositories/itemsRepository.js` (`listarComportamentos`, `listarComportamentosDosItens`, `listarUpgradesDe`), `src/repositories/inventoryRepository.js` (`listarParaCiclo`, `aplicarCicloDeValor` com piso e teto na própria instrução), `src/services/economicCycleService.js` (`aplicarValores`, `creditarRenda` e `cobrarCustoFixo`, nessa ordem) | `test/integration/repositories/economia.test.js` — videogame como `deprecia` + `custo_fixo`, cosmético como `neutro`, comportamentos de vários itens numa consulta só, depreciação de 4%, piso de 20% e teto de 200%; `test/integration/cicloEconomico.test.js` — o terreno que valoriza e a moto que deprecia no mesmo ciclo, e a renda da barraquinha creditada por ciclo | atendido |
| **RN-037** — sem saldo para o custo fixo o item fica inadimplente, e depois de 2 ciclos é vendido por 50% | `src/repositories/inventoryRepository.js` (`marcarInadimplente`, `regularizar`, `listarInadimplentesVencidas`, `marcarComoVendido`), `src/services/economicCycleService.js` (`cobrarCustoFixo` debita pelo `walletsRepository` porque faltar mel aqui não é erro, e `venderInadimplentesVencidas` devolve 50% do valor atual), `migrations/005_economy_items_inventory.sql` (`inventory.overdue_cycles`), `scripts/seeds/02_age_bands_domains.sql` (status `inadimplente`), `src/services/profilesService.js` (`regrasEconomicasDoUsuario` desliga a inadimplência onde não há custo fixo) | `test/integration/repositories/economia.test.js` — a inadimplência conta ciclos e a unidade continua no ciclo seguinte, a lista de quem passou do limite, e o retorno para ativa com o atraso zerado; `test/integration/cicloEconomico.test.js` — sem mel a carteira fica em zero e o item vira inadimplente, dois ciclos devendo vendem o item com o mel voltando inteiro, e pagar o custo tira da inadimplência; `test/integration/aceiteDaEconomia.test.js` | atendido — a venda forçada é explicada na Colmeia com o motivo e o valor |
| **RN-038** — na Faixa A (6–8 anos) não existem custo fixo, depreciação nem inadimplência | `migrations/016_faixa_sem_depreciacao.sql` (`age_bands.is_depreciation_enabled`), `scripts/seeds/02_age_bands_domains.sql` (a Faixa A com os dois interruptores em 0), `src/repositories/profilesRepository.js` (`buscarFaixaDoUsuario`), `src/services/profilesService.js` (`regrasEconomicasDoUsuario`, ponto único de leitura), `src/services/economicCycleService.js` (depreciação filtrada, custo fixo pulado com perdão de quem devia, venda forçada desligada), `src/services/shopService.js` (`comportamentoNaFaixa`, com custo zerado e a frase que explica) | `test/integration/faixaNaEconomia.test.js` — 7 casos: os interruptores por faixa, a mesma moto que deprecia e cobra na C e não faz nada na A, a Faixa A sem mel que não fica inadimplente nem perde o item, valorização e renda que continuam pagando, a dívida perdoada na mudança de faixa e a loja das duas faixas | atendido — falta a tela mostrar a frase, que é a T-09.7 |
| **RN-039** / **RN-041** — patrimônio é carteira + cofre + bens, e cosmético fica de fora | `src/services/patrimonyService.js` (`obterDoUsuario` soma na hora, sem total em cache; `guardarFotoDoDia` grava só quando o dia não tem foto ou o total mudou), `src/repositories/inventoryRepository.js` (`valorTotalEmPatrimonio`, filtrando por `counts_in_patrimony`), `src/repositories/patrimonyRepository.js` (`gravar`, `buscarDoDia`, `listarUltimas`), `src/repositories/vaultsRepository.js` (`buscarPorUsuario`, que não cria cofre na leitura), `src/services/contentService.js` (o requisito de favo da RN-028 passou a medir o patrimônio inteiro), `migrations/005_economy_items_inventory.sql` (`patrimony_snapshots`) | `test/integration/patrimonio.test.js` — patrimônio zero sem criar cofre, bem comprado que não muda o total, cosmético que só diminui, mel do cofre somado, e a foto do dia gravada uma vez e reescrita quando o total muda; `test/integration/repositories/economia.test.js`; `test/integration/repositories/inventory.test.js`; `test/integration/aceiteDaEconomia.test.js` | atendido — a soma é do service e é auditável; falta só a tela mostrar (DT-57) |
| **RN-032** / **RF-LOJ-04** — a compra debita mel, grava `price_at_purchase` e cria a linha no inventário, tudo em transação | `src/services/purchasesService.js` (`registrarCompra`: débito, compra e inventário na mesma transação), `src/repositories/purchasesRepository.js` (`criar`, com `discount_applied` e `total_price`), `src/repositories/inventoryRepository.js` (`adicionar`), `src/controllers/purchasesController.js`, `src/routes/loja.js` | `test/integration/loja.test.js` — compra sem mel recusada sem deixar rastro, e o preço de tabela congelado na linha da compra; `test/integration/fluxoAutenticado.test.js` — "comprar debita o mel, guarda o preço e entrega a unidade"; `test/integration/aceiteDaEconomia.test.js` | atendido |
| **RN-033** / **RF-LOJ-06** — item pode exigir nível, item anterior ou patrimônio mínimo, e o bloqueio diz o que falta | `src/services/itemsService.js` (`requisitosNaoCumpridosDosItens`, avaliação em lote; `avaliarRequisito` cobre nível, item pré-requisito e patrimônio mínimo), `src/repositories/itemsRepository.js` (`listarRequisitosDosItens`), `src/services/purchasesService.js` (`exigirRequisitos`) | `test/integration/loja.test.js` — requisito de item não cumprido barra antes do débito, e a vitrine devolve os bloqueios de cada item | parcial — nível, item e patrimônio bloqueiam de verdade; `favo-concluido` continua voltando como aviso até a E05 |
| **RF-LOJ-07** — upgrade de item com desconto pelo item anterior | `src/services/purchasesService.js` (`resolverTroca`, travando a unidade com `FOR UPDATE` dentro da transação), `src/repositories/inventoryRepository.js` (`bloquearUnidadeAtiva`, `marcarComoVendido`), `src/services/shopService.js` (`ofertaDeTroca`), `scripts/seeds/03_items_catalog.sql` (`upgrade_of_item_id`) | `test/integration/loja.test.js` — o desconto é o valor atual da unidade entregue, o patrimônio antes e depois é o mesmo, a unidade entregue sai do inventário, entrada de item errado é recusada e o mesmo upgrade enviado duas vezes compra uma vez só | atendido |
| **RF-LOJ-01** / **RF-LOJ-05** — saldo e patrimônio no topo da loja, e confirmação explicando o impacto da compra | `src/views/pages/loja.ejs`, `src/views/pages/confirmar-compra.ejs`, `src/views/partials/ui/patrimonio-topo.ejs`, `src/views/partials/ui/item-card.ejs`, `src/public/js/graficos.js`, `src/controllers/paginaController.js` (`loja`, `confirmarCompra`), `src/services/shopService.js` (`listarVitrine`, `previaDaCompra` com `patrimonioDepois`, `custoSemanal` e `rendaSemanal`), `src/controllers/shopController.js`, `src/routes/loja.js` (`GET /loja/itens`, `GET /loja/itens/:idItem/previa`) | `test/integration/loja.test.js` — a vitrine mostra o desconto da troca e o que falta de mel, e a prévia mostra o impacto antes de confirmar; `test/integration/fluxoAutenticado.test.js` — "a prévia explica o impacto da compra antes de confirmar" `test/integration/telasDaEconomia.test.js` | atendido — a vitrine agrupa por categoria, o patrimônio fica no topo e a confirmação diz para onde vai o patrimônio antes de a criança gastar |
| **RF-INV-02** / **RF-INV-04** — inventário separa bens de cosméticos e mostra a composição do patrimônio | `src/views/pages/inventario.ejs`, `src/views/partials/ui/patrimonio-topo.ejs`, `src/public/js/graficos.js`, `src/services/inventoryService.js` (`resumoDoUsuario`, `listarAgrupadoPorItem` com `contaNoPatrimonio`, `valorPago`, custo e renda semanais), `src/repositories/inventoryRepository.js` (join com `purchases` para o valor pago), `src/controllers/inventoryController.js` | `test/integration/patrimonio.test.js` — "o inventário separa bens de cosméticos e traz a composição"; `test/integration/fluxoAutenticado.test.js` — o item comprado aparece no inventário `test/integration/telasDaEconomia.test.js` | atendido — bens e enfeites em blocos separados, com a composição desenhada e escrita ao lado |
| **RF-LOJ-02** / **RF-LOJ-03** / **RF-INV-01** / **RF-INV-03** — catálogo por categoria, card com comportamento em linguagem infantil, e o inventário com valor pago, valor atual, variação, renda e custo | `src/views/pages/loja.ejs` (agrupamento por categoria), `src/views/partials/ui/item-card.ejs` (as frases do comportamento), `src/views/pages/inventario.ejs` (pagou, vale hoje, variação, renda e custo por grupo), `src/services/inventoryService.js` (`listarAgrupadoPorItem`) | `test/integration/telasDaEconomia.test.js` — as categorias na vitrine, as frases de perde valor, rende por semana e enfeite, e o inventário com os três números por item | atendido |
| **RF-COF-01** / **RF-COF-02** — depositar e sacar mel, com rendimento por ciclo e extrato | `src/services/vaultService.js` (`depositar`, `sacar`, `aplicarRendimento`, tudo em transação com o cofre travado), `src/repositories/vaultsRepository.js` (`bloquearPorUsuario`, `creditar`, `debitar` com `WHERE balance >= ?`, `registrarTransacao`, `listarTransacoes`), `src/controllers/vaultController.js`, `src/routes/cofre.js` | `test/integration/cofre.test.js` — depósito que não muda o total do jogador, saque que devolve à carteira, saque acima do saldo recusado sem rastro, e o `balance_after` de cada linha do extrato; `test/integration/fluxoAutenticado.test.js` — "guardar e tirar mel do cofre pelas rotas do cofre"; `test/integration/telasDaEconomia.test.js` — o formulário da página guarda mel e volta para `/cofre` com o extrato atualizado | atendido |
| **RN-042** / **RN-043** — o cofre rende 2% por ciclo, configurável em banco, e o mel sacado não rende no ciclo do saque | `src/services/vaultService.js` (`aplicarRendimento`, base igual ao saldo menos o sacado, com piso em zero), `src/repositories/vaultsRepository.js` (`totalSacadoDesde`, cortando o instante no segundo cheio), `migrations/005_economy_items_inventory.sql` (`vaults.interest_rate`, default 2.000) | `test/integration/cofre.test.js` — "o rendimento do ciclo paga 2% e entra no extrato" e "o mel sacado no ciclo não rende naquele ciclo" | atendido — quem chama uma vez por ciclo é o `economicCycleService`, coberto em `test/integration/cicloEconomico.test.js` ("o cofre rende no ciclo e o extrato guarda quanto rendeu") |
| **RN-044** / **RF-COF-03** — meta de cofre com valor e prazo, e bônus de mel ao bater | `src/services/vaultService.js` (`definirMeta`, `pagarMetaSeBatida`, conferida no depósito e no rendimento), `src/repositories/vaultsRepository.js` (`definirMeta`), `scripts/seeds/07_reward_modifiers.sql` (`bonus-de-meta-do-cofre`, 5% do alvo) | `test/integration/cofre.test.js` — "bater a meta paga o bônus uma vez só e libera a próxima" `test/integration/telasDaEconomia.test.js` — a meta declarada pelo formulário da página | parcial — o valor, o bônus e a limpeza da meta funcionam; o prazo é guardado mas nada acontece quando vence (DT-58) |
| **RF-COF-04** — projeção do cofre ("guardando X por semana, em N semanas terá Y") | `src/services/vaultService.js` (`projetar` e `semanasParaAlcancar`, conta pura com teto de dez anos) | `test/unit/vaultService.test.js` — 7 casos sem banco, fixando que o rendimento vem antes do depósito da semana e que a fração de mel é truncada; `test/integration/telasDaEconomia.test.js` | atendido — a projeção tem tabela e gráfico na tela do cofre |
| **RN-046** — tarefa `depositar-no-cofre` mede mel guardado na janela da tarefa | `src/services/taskProgressSources.js` (fonte `vault_deposit`), `src/repositories/vaultsRepository.js` (`totalDepositadoEntre`), `scripts/seeds/02_age_bands_domains.sql` (a tarefa voltou a nascer ativa) | `test/integration/tarefasDoDia.test.js` — "só propõe tarefa cuja fonte o sistema sabe medir"; `test/integration/fluxoAutenticado.test.js` — a tarefa do cofre é cumprida depositando de verdade | atendido — paga a DT-43 |
| **RF-HOM-01** / **RF-HOM-02** / **RF-HOM-03** — nível com barra de XP, saldo de mel e patrimônio, e a sequência com os dias marcados da semana | `src/services/homeService.js` (`obterColmeia`, que junta perfil, patrimônio e resumo da semana num bloco `jogador` só), `src/services/patrimonyService.js`, `src/services/streakService.js` (`resumoDaSemana`), `src/views/partials/ui/cabecalho-colmeia.ejs` (topo grudado, fora do cartão que tem `overflow-hidden`), `src/views/partials/ui/badge-recurso.ejs` e `barra-progresso.ejs` (também usados na loja, no inventário e no cofre), `src/views/pages/painel.ejs`, `src/controllers/paginaController.js` (`painel`) | `test/integration/cabecalhoDaColmeia.test.js` — 5 casos pelo HTTP: os quatro números com ícone e palavra, a barra anunciando `aria-valuenow` com a largura em classe e sem `style` na marcação, o cabeçalho vindo antes do cartão que desligaria o `sticky`, a semana desenhada uma vez só e a composição do patrimônio escrita; `test/integration/colmeia.test.js` — "responde os blocos da home numa chamada só", com o patrimônio conferido contra a soma da RN-039 | atendido — falta o passe de olho humano a 320 px (DT-22) |
| **RF-HOM-04** / **RF-HOM-05** / **RF-MET-02** — meta com vencimento mais próximo (título, percentual, dias restantes e mel da recompensa), lista resumida das outras e as metas listadas com percentual e prazo | `src/services/goalsService.js` (`resumirMeta`, `ordenarPorVencimento` e `urgenciaDoPrazo`, contas puras que tiraram a aritmética de meta do EJS e traduzem o prazo em palavra), `src/services/homeService.js` (escolhe destaque e outras), `src/views/partials/ui/card-meta.ejs` e `estado-vazio.ejs`, `src/views/pages/painel.ejs`, `src/views/pages/metas.ejs`, `src/controllers/paginaController.js` (`metas`) | `test/unit/resumoDeMetas.test.js` — 16 casos sem banco: percentual com teto de 100, alvo zero que não vira divisão por zero, meta sem prazo devolvendo `null`, prazo vencido que não vira número negativo e os quatro níveis de urgência; `test/integration/metaDaColmeia.test.js` — 5 casos pelo HTTP: o card com os quatro dados, a lista curta com no máximo duas barras na página, "Termina hoje" sem "Faltam 0 dias", o mesmo card em `/metas` com a ação de cada estado e o estado vazio; `test/integration/colmeia.test.js` — o destaque muda de dono sozinho quando outra meta passa a vencer antes | atendido |
| **RF-HOM-06** — trilha de favos em hexágonos com estado (bloqueado, atual, concluído) | `src/services/homeService.js` (`marcarFocoDaTrilha`, que destaca o favo em andamento e o seguinte), `src/services/contentService.js` (`listarTrilha`, dona das regras RN-026 a 028), `src/views/partials/ui/favo-card.ejs` (modo compacto para os favos fora do foco), `src/views/pages/painel.ejs`, `src/styles/trilha.css` | `test/unit/focoDaTrilha.test.js` — 6 casos sem banco: o foco no favo em andamento e no seguinte, o concluído que não rouba o foco, o último favo em foco sozinho, a trilha toda concluída sem foco nenhum e o favo original que não é alterado; `test/integration/trilhaDaColmeia.test.js` — 5 casos pelo HTTP: um hexágono por favo visível, no máximo dois em foco, barra só nos que estão em foco, o travado sem link e com o motivo escrito, e a página sem `style` na marcação | atendido — falta o passe de olho humano a 320 px (DT-22) |
| **RF-HOM-08** — tarefas do dia na Colmeia | `src/services/tasksService.js` (`resumirTarefa`, que responde percentual, `cumprida` e `concluida`), `src/services/homeService.js` (`tarefas` já resumidas), `src/views/partials/ui/card-tarefa.ejs` (compacto na home, com barra em `/metas`), `src/views/pages/painel.ejs`, `src/views/pages/metas.ejs`, `src/controllers/tasksController.js` e `src/utils/resposta.js` (`paginaDeVolta`, lista branca do destino do redirecionamento) | `test/unit/resumoDeMetas.test.js` — 3 casos do resumo da tarefa: percentual, alvo ultrapassado que continua cumprido e tarefa já concluída; `test/integration/tarefasDaColmeia.test.js` — 5 casos pelo HTTP: a tarefa do dia com progresso escrito, a em andamento sem botão, a cumprida recebida na Colmeia com volta para a Colmeia, o destino fora da lista branca caindo no padrão e a barra que só `/metas` desenha | atendido |
| **RF-HOM-07** — botão "Continuar" que leva à próxima célula pendente | `src/services/contentService.js` (`proximaCelulaPendente`, que recebe a trilha já lida e olha só o primeiro favo aberto e não concluído; `celulasDoFavo` extraída para os dois caminhos usarem a mesma leitura) | `test/integration/colmeia.test.js` — "o Continuar aponta para a primeira célula jogável do favo em aberto", conferido contra o que `listarCelulasDoFavo` devolve | parcial — o destino existe e o botão é a T-10.6 |
| **RNF-04** — nenhuma consulta N+1 nas telas de Colmeia, Loja e Inventário | `src/services/homeService.js` (um `Promise.all` por visita, sem laço com consulta dentro), `src/services/contentService.js` (`proximaCelulaPendente` recebendo a trilha pronta em vez de relê-la) | `test/integration/colmeia.test.js` — "o número de consultas não cresce quando o jogador acumula item": o teste instrumenta `pool.execute`, mede uma visita, acrescenta doze unidades ao inventário e exige o mesmo número depois | atendido para a Colmeia; loja e inventário ainda sem a mesma medição |
| **RNF-01** — resposta de página em até 2 s | `src/services/homeService.js`, `src/controllers/paginaController.js` (`painel`) | `test/integration/colmeia.test.js` — "a Colmeia responde dentro do teto de 2 s da RNF-01"; medido também com o servidor de pé e a conta demo: 171 ms em HTML e 274 ms em JSON | atendido |
